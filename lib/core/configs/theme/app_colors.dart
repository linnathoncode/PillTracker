import 'package:flutter/material.dart';

class AppColors {
  // temporary
  static const primary = Colors.green;
  static const lightSurface = Color.fromARGB(255, 243, 241, 223);
  static const darkSurface = Color.fromARGB(255, 42, 42, 42);
}
